{
  "name": "server",
  "type": "module",
  "scripts": {
    "lint": "biome check --write",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules dist api/*.js api/*.js.map",
    "dev": "NODE_ENV=development pnpm with-env tsx watch src",
    "with-env": "dotenv -e ../../.env --",
    "build": "pnpm with-env tsup",
    "start": "pnpm with-env node api/index.js"
  },
  "dependencies": {
    "@clerk/backend": "^2.29.0",
    "@hono/node-server": "^1.19.6",
    "@hono/trpc-server": "^0.4.0",
    "@t3-oss/env-core": "catalog:",
    "api": "workspace:*",
    "hono": "^4.11.7",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "^20.19.25",
    "@vercel/node": "^5.5.14",
    "dotenv-cli": "catalog:",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "ultracite": "catalog:"
  }
}
