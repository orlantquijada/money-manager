{
  "name": "api",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "default": "./index.ts"
    },
    "./transformer": {
      "default": "./transformer.ts"
    }
  },
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist",
    "lint": "biome check --write",
    "type-check": "tsc --noEmit",
    "build": "tsup index.ts --format cjs --minify"
  },
  "dependencies": {
    "@clerk/backend": "^2.29.0",
    "@date-fns/tz": "^1.4.1",
    "@google/generative-ai": "^0.21.0",
    "@trpc/client": "catalog:",
    "@trpc/server": "catalog:",
    "date-fns": "catalog:",
    "db": "workspace:*",
    "drizzle-orm": "catalog:",
    "superjson": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/pg": "catalog:",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "ultracite": "catalog:"
  }
}
